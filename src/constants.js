const packageName = 'plugin-chat-sms-transfer';

const pluginConfig = {};

const configForm = {
  title: 'Chat Transfer Settings',
  type: 'object',
  properties: {},
};

const uiSchema = {};
export { packageName, pluginConfig, configForm, uiSchema };
